# Convierte el campo "engines" de package.json en un requisito real: npm falla
# la instalacion si la version de Node no lo cumple, en vez de limitarse a
# avisar. Sin esto, alguien puede instalar con una Node incompatible y descubrir
# el problema varios pasos mas tarde, con un error que no menciona la version.
engine-strict=true
